#include <stdio.h>
#include <stdlib.h> // for exit()
#include "../lib/run.h"

int initials(FILE *fp, char *caseFolder) {
  fprintf(fp, "\n%s\n", "|----------------------|\n PHYSICS - INITIAL CONDITIONS");
  return EXIT_SUCCESS;
}
